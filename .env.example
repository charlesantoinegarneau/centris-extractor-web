# Python Backend Configuration
# Uncomment and configure the URL where your Python extraction service is deployed

# Local development (if running FastAPI locally)
# PYTHON_BACKEND_URL=http://localhost:8001

# Production deployment options:
# PYTHON_BACKEND_URL=https://your-app.onrender.com
# PYTHON_BACKEND_URL=https://your-app.railway.app  
# PYTHON_BACKEND_URL=https://your-app.herokuapp.com

# If not set, the app will use enhanced demo data with all 11 columns
# Set to enable real PDF extraction via external Python service